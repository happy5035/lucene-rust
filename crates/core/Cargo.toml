[package]
name = "rustlucene-core"
version = "0.1.0"
edition = "2021"

[dependencies]
codec-lucene9 = { path = "../codec-lucene9" }
serde_json = { version = "1.0.151", features = ["preserve_order"] }

[dev-dependencies]
crc32fast = "1.5.0"
