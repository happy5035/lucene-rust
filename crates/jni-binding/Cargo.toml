[package]
name = "rustlucene-jni"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
jni = "0.21"
rustlucene-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
